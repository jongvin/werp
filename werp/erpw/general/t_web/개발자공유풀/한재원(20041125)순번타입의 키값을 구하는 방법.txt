//레코드 입력시에 키값을 가져와서 그리드의 키 칼럼에 넣어줄때 사용한다.
//DB에서 Sequence를 사용하지 않고 그리드 칼럼 MAX값 속성을 사용해서 구하는 방법.
제한사항-입력자가 단독일때 사용하는 프로그램에서 사용해주세요.
입력자(사용자가)가 여러명일때는 Sequence를 사용하고 코드성의 데이트를 입력하는 프로그램에서 사용해주세요.
function btnInsert_onClick()
{
			
	G_addRow(dsMAIN)
	txtAccName.value="";
	txtBankName.value="";
			    
	var num = dsMAIN.Max(1,0,0);
	dsMAIN.ColumnValue(dsMAIN.RowPosition,1)=parseInt(num) + 1			
}