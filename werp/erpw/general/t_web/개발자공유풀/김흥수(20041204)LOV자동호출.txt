이 함수를 호출하기전에 먼저 arDataset에 해당되는 데이타셑을 아래와 같이 Initialize()에서 초기화 해야 한다.
			function Initialize()
			{
				G_addDataSet(dsSEARCH, null, null, "", "LOV");
			}
이 함수를 부르는 방법은
				var lrArg = new C_Dictionary();
				lrArg.set("COMPANY_CODE","100");
				lrArg.set("PROJ_NAME","전체");
				var		lrRet = C_AutoLov(dsSEARCH,strNAME,lrArg);
				if(lrRet == null)
				{
					alert("null");
				}
				else
				{
					alert(lrRet.get("PROJ_NAME"));
				}
이런 식입니다.