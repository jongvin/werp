		UPDATE y_budget_detail  a
		  SET (a.mat_code ) = 
						 ( SELECT b.mat_code
							  from y_stand_detail b
							 where a.detail_code    = b.detail_code
								and b.detail_code like 'EKAA%')
		 WHERE EXISTS (SELECT b.mat_code
							  from y_stand_detail b
							 where a.detail_code    = b.detail_code
								and b.detail_code like 'EKAA%');
		UPDATE y_chg_budget_detail  a
		  SET (a.mat_code ) = 
						 ( SELECT b.mat_code
							  from y_stand_detail b
							 where a.detail_code    = b.detail_code
								and b.detail_code like 'EKBA%')
		 WHERE EXISTS (SELECT b.mat_code
							  from y_stand_detail b
							 where a.detail_code    = b.detail_code
								and b.detail_code like 'EKBA%');
		UPDATE y_budget_detail  a
		  SET (a.mat_code ) = 
						 ( SELECT b.mat_code
							  from y_stand_detail b
							 where a.detail_code    = b.detail_code
								and b.detail_code like 'EKBA%')
		 WHERE EXISTS (SELECT b.mat_code
							  from y_stand_detail b
							 where a.detail_code    = b.detail_code
								and b.detail_code like 'EKBA%');

commit work;